# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'toy-specta-expecta-gol' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!

  # Pods for toy-specta-expecta-gol

  target 'toy-specta-expecta-golTests' do
    inherit! :search_paths
    # Pods for testing
    pod 'Specta'
    pod 'Expecta'
    pod 'OCMock'
  end

  target 'toy-specta-expecta-golUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
